<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">
	<select id="selectBoardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT 
    B.*, 
    DECODE(
        TRUNC(CDATETIME), 
        TRUNC(SYSDATE), TO_CHAR(CDATETIME, 'HH24:MI:SS'),  <!--첫 번째 값이 TRUNC(SYSDATE)이면 시간, 아니면 날짜-->
        TO_CHAR(CDATETIME, 'YYYY-MM-DD')
    ) AS CDATE,
    NVL(CCNT, 0) AS COMMENTCNT
	FROM TBL_BOARD B
	LEFT JOIN (
    SELECT BOARDNO, COUNT(*) AS CCNT
    FROM TBL_COMMENT
    GROUP BY BOARDNO
	) T ON T.BOARDNO = B.BOARDNO

		WHERE 1=1
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		
		<if test="searchOption == 'all'">
			AND (
				TITLE LIKE '%' || #{keyword} || '%' OR
				USERID LIKE '%' || #{keyword} || '%'
			)
		</if>
		<if test="searchOption == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchOption == 'id'">
			AND USERID LIKE '%' || #{keyword} || '%'
		</if>
		
		<if test="order == 'num'">
			ORDER BY B.BOARDNO ASC
		</if>
		<if test="order == 'title'">
			ORDER BY TITLE ASC
		</if>
		<if test="order == 'cnt'">
			ORDER BY CNT DESC
		</if>
		<if test="order == 'time'">
			ORDER BY CDATE ASC
		</if>
		OFFSET #{page} ROWS FETCH NEXT #{pageSize} ROWS ONLY
		
	</select>
	
	<select id="selectBoardCnt" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM TBL_BOARD B
		WHERE 1=1
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		
		<if test="searchOption == 'all'">
			AND (
				TITLE LIKE '%' || #{keyword} || '%' OR
				USERID LIKE '%' || #{keyword} || '%'
			)
		</if>
		<if test="searchOption == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchOption == 'id'">
			AND USERID LIKE '%' || #{keyword} || '%'
		</if>
		
	</select>
	
	<delete id="deleteBoard" parameterType="hashmap">
		DELETE FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</delete>
	
	<insert id="insertBoard" parameterType="hashmap">
		INSERT INTO TBL_BOARD 
		VALUES(B_SEQ.NEXTVAL, #{title}, #{contents}, #{userId}, 0, 0, 1 , SYSDATE, SYSDATE)
	</insert>
	
	<select id="selectBoard" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT *
		FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</select>
	
	<select id="selectCommentList" parameterType="hashmap" resultType="com.example.test1.model.Comment">
		SELECT C.*, U.NICKNAME
		FROM TBL_COMMENT C
		INNER JOIN TBL_USER U ON C.USERID = U.USERID
		WHERE C.BOARDNO = #{boardNo}
		ORDER BY C.CDATETIME ASC
	</select>
	
	<insert id="insertComment" parameterType="hashmap">
		INSERT INTO TBL_COMMENT 
		VALUES(COMMENT_SEQ.NEXTVAL, #{boardNo}, #{contents}, #{id} , 0, NULL, SYSDATE, SYSDATE)
	</insert>
	
	<update id="updateCnt" parameterType="hashmap">
		UPDATE TBL_BOARD 
		SET 
			CNT = CNT + 1
		WHERE BOARDNO = #{boardNo}
	</update>
</mapper>



