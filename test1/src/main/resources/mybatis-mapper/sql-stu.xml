<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.StuMapper">
	<select id="stuInfo" parameterType="hashmap" resultType="com.example.test1.model.Student">
		SELECT S.STU_NO, S.STU_NAME, STU_DEPT, AVG(ENR_GRADE) AS AVG_GRADE
		FROM STUDENT s
		LEFT JOIN ENROL E ON S.STU_NO = E.STU_NO
		WHERE S.STU_NO = #{stuNo}
		GROUP BY S.STU_NO, S.STU_NAME, STU_DEPT
		<!--보낸 이름인 keyword로 -->
	</select>
	
	<select id="stuList" parameterType="hashmap" resultType="com.example.test1.model.Student">
		SELECT *
		FROM STUDENT
	</select>
	
	<delete id="deleteStu" parameterType="hashmap">
		DELETE 
		FROM STUDENT
		WHERE STU_NO = #{stuNo}
	</delete>
	
	<update id="editStu" parameterType="hashmap">
		UPDATE STUDENT
		SET STU_NAME = #{stuName},
			STU_DEPT = #{stuDept}
		WHERE STU_NO = #{stuNo}
	</update>
</mapper>




